# Web

React + TypeScript + Vite 前端，负责：

- 首页与 `Recent Records`
- `WaterBase Mount Workspace`
- `Overseas PV Structural Technical Framework Workspace`
- 通用记录详情页
- 前端状态机与保存/导出按钮控制

本地开发：

```bash
npm install
npm run dev -- --host 127.0.0.1 --port 5173
```
